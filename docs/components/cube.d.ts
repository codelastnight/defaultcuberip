import * as BABYLON from "babylonjs";
/**
 * creates da cube
 * @param scene the scene
 * @param camera the camera ( for calculating outline width)
 */
export declare const createCube: (scene: BABYLON.Scene, camera: BABYLON.ArcRotateCamera) => BABYLON.Mesh;
